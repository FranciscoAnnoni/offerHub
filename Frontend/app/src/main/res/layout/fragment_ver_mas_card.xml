<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/btnVerMas"
    android:layout_width="120dp"
    android:layout_height="170dp"
    android:layout_rowWeight="1"
    android:layout_columnWeight="1"
    android:background="@drawable/border_promocion"
    android:backgroundTint="@color/g_light_purple"
    android:layout_marginLeft="20dp"
    android:orientation="vertical"
    android:padding="5dp"
    android:gravity="center"
    android:textColor="@color/black">

    <ImageView
        android:id="@+id/verMasIcono"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/ic_arrow"
        app:tint="@color/black"
        android:layout_gravity="center"
        app:layout_constraintBottom_toTopOf="@id/txtCategoria"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/txtCategoria"
        style="@style/blueButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ver Más"
        android:textColor="@color/black"
        android:textSize="16dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_gravity="center"
        app:layout_constraintTop_toBottomOf="@id/verMasIcono"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
